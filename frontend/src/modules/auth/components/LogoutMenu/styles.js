const styles = () => ({
  wrapper: {
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
    cursor: "pointer"
  },
  name: {
    color: "#fff",
    fontSize: 14,
    textTransform: "uppercase",
    marginBottom: 0,
    marginRight: 10
  },
  icon: {
    fontSize: 28,
    color: "#fff"
  }
});

export default styles;
